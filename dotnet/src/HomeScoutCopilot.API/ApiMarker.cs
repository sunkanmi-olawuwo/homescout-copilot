namespace HomeScoutCopilot.API;

/// <summary>
/// Public marker type in the API assembly. Used by the test project as the
/// <c>WebApplicationFactory</c> entry-point generic so the API can be booted
/// in-memory without clashing with the AppHost's own <c>Program</c> type.
/// </summary>
public sealed class ApiMarker;
