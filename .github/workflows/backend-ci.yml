name: backend-ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore dotnet/HomeScoutCopilot.slnx

      - name: Build
        run: dotnet build dotnet/HomeScoutCopilot.slnx --no-restore --configuration Release

      - name: Test (fast, non-integration)
        run: >-
          dotnet test dotnet/HomeScoutCopilot.slnx --no-build --configuration Release
          --filter "Category!=Integration"
